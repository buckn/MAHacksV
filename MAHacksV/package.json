{
  "name": "KibbleCalc",
  "displayName": "KibbleCalc",
  "version": "1.0.0",
  "description": "A Cordova application that calculates the amount of calories any given dog needs. It also gives statistics about all the dogs entered into our system.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "buckn",
  "license": "Apache-2.0",
  "dependencies": {
    "cordova-browser": "^6.0.0",
    "cordova-electron": "^1.1.1",
    "cordova-ios": "^5.0.1"
  },
  "devDependencies": {
    "cordova-plugin-whitelist": "^1.3.4"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {}
    },
    "platforms": [
      "browser",
      "ios",
      "android",
      "electron"
    ]
  }
}